diff --git a/server/src/routes/archive.rs b/server/src/routes/archive.rs
index 350bf76..e959bf9 100644
--- a/server/src/routes/archive.rs
+++ b/server/src/routes/archive.rs
@@ -68,7 +68,17 @@ async fn resolve_path(url: &str) -> Result<PathBuf, StatusCode> {
         }
 
         let mut content = response.bytes_stream();
-        let temp_file = tempfile::NamedTempFile::new().map_err(|e| {
+        // The archive readers detect the format from the file extension, so
+        // the download keeps the URL's (a bare temp name matched none).
+        let suffix = url::Url::parse(url)
+            .ok()
+            .and_then(|parsed| {
+                std::path::Path::new(parsed.path())
+                    .extension()
+                    .map(|ext| format!(".{}", ext.to_string_lossy().to_lowercase()))
+            })
+            .unwrap_or_default();
+        let temp_file = tempfile::Builder::new().suffix(&suffix).tempfile().map_err(|e| {
             tracing::error!("Failed to create temp file: {}", e);
             StatusCode::INTERNAL_SERVER_ERROR
         })?;
